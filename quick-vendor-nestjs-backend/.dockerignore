node_modules
dist
.git
.gitignore
*.md
coverage
.vscode
.idea
.claude

# Env files are NOT copied into the image.
# They are mounted at runtime via docker-compose env_file.
.env
.env.*
!.env.example

# Local uploads are runtime volumes, not build artifacts.
uploads
